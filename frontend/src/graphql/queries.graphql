query getUserLogin($email: String!) {
    user(where: { email: $email }) {
        id
        alias
        email
        createdAt
        updatedAt
    }
}

query getUsers {
    users {
        id
        alias
        email
    }
}